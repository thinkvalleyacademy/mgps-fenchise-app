# Copy this file to .env in the same folder on the Linux server.

DB_NAME=mgps_master
DB_USERNAME=postgres
DB_PASSWORD=postgres123
DB_PORT=5432
DB_HOST=postgres

# Path to the code repository on the Linux server.
CODE_DIR=/home/mgps01/mgpsv2/code/mgpsfren/mgps-fenchise-app

PROXY_PORT=6080
FRONTEND_PORT=80

# Host directory that application/nginx logs are written to (bind-mounted, not
# a docker volume, so they're readable directly on the server). Must match
# APP_LOG_DIR in docker-files/monitoring-logging/.env if you're running that
# stack's Filebeat against these same logs.
APP_LOG_DIR=/var/log/mgps

# Frontend must call the backend URL accessible from the browser.
# When using the Nginx reverse proxy, keep this as /api so the browser calls
# the same host and the proxy forwards it to the backend container.
VITE_API_BASE_URL=/api

# Spring Boot / JWT settings.
# The backend refuses to start if either of these is left unset. Generate a
# real value for each with: openssl rand -base64 32
JWT_SECRET=
ENCRYPTION_KEY=
APP_CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:80,http://YOUR_SERVER_IP,http://YOUR_SERVER_IP:80
