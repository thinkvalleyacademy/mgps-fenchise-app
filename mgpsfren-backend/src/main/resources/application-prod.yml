spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:mgps_master}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres123}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:https://mgpsfren.thinkvalleysoftwares.in,http://100.101.103.63:6080,https://mothersgoose.in,http://localhost:8081}

logging:
  level:
    root: INFO
    com.mgps: INFO
    org.hibernate.SQL: INFO
    org.springframework.security: INFO
